[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

[rpc_endpoints]
monad_testnet = "${MONAD_RPC_URL}"

[etherscan]
monad = { key = "${ETHERSCAN_API_KEY}", url = "${BLOCK_EXPLORER_URL}" }
